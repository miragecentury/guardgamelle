{%extends "::layout.html.twig" %}

{% block title %}{{ parent() }} - Animal - Race - Ajout{% endblock %}

{% block subnavbar %}
{% endblock %}

{% block main %}
<div class="container">
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
            <div class="well">
                <form class=" form-horizontal" method="POST">
                    <div class=" form-group" style=" text-align: center;">
                        <h3>Voulez-vous réellement supprimer la race : "{{race.getNom()}}" ?</h3>
                    </div>
                    <div class=" form-group" style=" text-align: center;">
                        <input id="{{form._token.vars.id}}" type="hidden" name="{{form._token.vars.full_name}}" value="{{form._token.vars.value}}"/>
                        <input class="btn btn-danger" type="submit" value="Supprimer"/>
                        <a href="{{path("guard_common_animal_race")}}" class="btn btn-info">Annuler</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}